# Production Environment Configuration
# Used when building for Firebase Hosting deployment
#
# Build command: npm run build
# Deploy command: firebase deploy --only hosting

# Nexus Backend API (production)
VITE_NEXUS_API_URL=https://nexus-server.multifi.ai

# LangGraph API (if deployed to production)
# Leave empty if not using LangGraph in production
VITE_LANGGRAPH_API_URL=

# NOTE: API keys should NEVER be committed here
# Use .env.production.local (git-ignored) for sensitive values
