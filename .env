# Nexus Frontend Environment Configuration
# Local development setup

# Nexus API URL (for browser requests from local machine)
VITE_NEXUS_API_URL=http://localhost:8080

# Nexus Server URL (for LangGraph container-to-container communication)
# This is what gets passed to LangGraph agents in metadata
# LangGraph container will use this to connect to Nexus container
VITE_NEXUS_SERVER_URL=http://nexus:8080

# LangGraph API URL (for browser requests)
VITE_LANGGRAPH_API_URL=http://localhost:2024
