# Nexus Frontend Environment Configuration
# Copy this file to .env.local and customize for local development
#
# Usage:
#   cp .env.example .env.local
#   # Edit .env.local with your values
#
# Environment file precedence (highest to lowest):
#   1. .env.production.local (production build, git-ignored)
#   2. .env.production (production build)
#   3. .env.local (all modes, git-ignored)

# ============================================
# Nexus Backend API
# ============================================

# Nexus API URL (for browser requests)
#
# Development (npm run dev):
#   - Leave EMPTY to use Vite proxy → https://nexus-dev.nexilab.co
#   - OR set to: http://localhost:8080 (if running Nexus locally)
#
# Production (npm run build):
#   - Set in .env.production: https://nexus.nexilab.co
#
# Docker (docker-compose):
#   - Passed as build arg: http://localhost:8080
VITE_NEXUS_API_URL=

# Nexus API Key (optional, for authenticated requests)
# Leave empty if using public endpoints or testing without auth
VITE_API_KEY=

# ============================================
# LangGraph Service
# ============================================

# LangGraph API URL
# Development: http://localhost:2024 (if running LangGraph locally)
# Production: https://langgraph.nexilab.co (if deployed)
# Leave empty if not using LangGraph features
VITE_LANGGRAPH_API_URL=

# LangGraph API Key (optional, for LangSmith tracing)
# Get your key from: https://smith.langchain.com/settings
VITE_LANGGRAPH_API_KEY=

# ============================================
# Advanced: Docker Internal Networking
# ============================================

# Nexus Server URL for container-to-container communication
# Only used when building Docker images with docker-compose
# Default: http://nexus:8080 (Docker service name)
# You typically don't need to set this in .env.local
# VITE_NEXUS_SERVER_URL=http://nexus:8080

# ============================================
# Examples for Different Scenarios
# ============================================

# Example 1: Local development with Vite proxy (recommended)
# VITE_NEXUS_API_URL=
# VITE_LANGGRAPH_API_URL=

# Example 2: Local development with local Nexus server
# VITE_NEXUS_API_URL=http://localhost:8080
# VITE_LANGGRAPH_API_URL=http://localhost:2024

# Example 3: Local development with remote deployed server
# VITE_NEXUS_API_URL=https://nexus-dev.nexilab.co
# VITE_LANGGRAPH_API_KEY=lsv2_pt_your_key_here

# Example 4: Production testing locally before deploy
# VITE_NEXUS_API_URL=https://nexus.nexilab.co
